# openGym — standalone web server: serves the static app AND proxies /api to the backend,
# so the whole app lives on ONE origin (required for WebAuthn / passkeys).
server {
    listen ${NGINX_PORT};
    root /usr/share/nginx/html;
    index index.html;

    # API → backend container.
    # O bloco é montado por web/nginx-start.sh em ${PROXY_CONF}:
    #  - padrão (docker compose): proxy http direto para ${BACKEND}:${PORT}
    #  - USE_PUBLIC_UPSTREAM=1: proxy https para um domínio público da API
    #    (plataformas sem rede privada compartilhada, ex.: Railway/Vercel)
    location /api/ {
${PROXY_CONF}
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # App shell: always revalidate so updates show up immediately
    location ~* \.(js|css|json|html)$ {
        add_header Cache-Control "no-cache, must-revalidate";
    }

    # Exercise images/GIFs never change → cache hard
    location ~* \.(png|jpg|jpeg|gif|ico|svg|woff|woff2|webp|avif)$ {
        expires 30d;
        add_header Cache-Control "public, max-age=2592000, immutable";
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript image/svg+xml;
    gzip_vary on;
    gzip_min_length 1024;
}
